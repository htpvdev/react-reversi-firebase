{
  "configurations": [
    { /** client(Debug mode) */
      "type": "pwa-chrome",
      "request": "launch",
      "name": "client(Debug)",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/client",
      "preLaunchTask": "client-start",
      "postDebugTask": "terminate-client-start",
    },
    { /** client */
      "command": "cd ${workspaceFolder}/client; npm start",
      "name": "client",
      "request": "launch",
      "type": "node-terminal",
      "env": {}
    }
  ]
}