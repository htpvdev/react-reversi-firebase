{
  "configurations": [
    { /** client(Attach Debugger) */
      "name": "client(Debug)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
    },
    { /** client */
      "name": "client",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm start --prefix=${workspaceFolder}",
    }
  ]
}